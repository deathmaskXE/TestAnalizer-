# XE TESTER ANALYTICS
Panel privado separado del XE TESTER CONTROLLER LAB.

La interfaz está lista y no toca WebUSB, GIP, calibración ni test.

Siguiente paso: crear proyecto Firebase y pegar la configuración Web en firebase-config.js. Después conectaremos un emisor aislado desde el tester para page_visit, test_completed, calibration_completed y calibration_error.
